/**
 * .reverse() - на месте обращает порядок следования элементов массива. Первый элемент массива становится последним, а последний — первым.
 *
 * Параметры: .reverse()
 *
 * Примеры:
 */

const array = ["один", "два", "три"];
array.reverse();

console.log(array); // ['три', 'два', 'один']
