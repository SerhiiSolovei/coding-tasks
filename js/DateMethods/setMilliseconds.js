/**
 * - setMilliseconds() - устанавливает миллисекунды указанной даты по местному времени.
 * - Параметры - .setMilliseconds(ms)
 *
 *   - ms - Целое число от 0 до 999, представляющее миллисекунды.
 *
 * Пример:
 */

const theBigDay = new Date();
theBigDay.setMilliseconds(500);
